{
  "name": "blabla",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "build": "bun run build.ts",
    "serve": "bunx live-server ./dist",
    "start": "NODE_ENV=production bun index.ts",
    "stats": "bun run build && bunx esbuild-visualizer --metadata ./dist/meta.json --filename ./dist/stats.html --open",
    "outdated": "bunx npm-check-updates --interactive"
  },
  "dependencies": {
    "@preact/signals": "^2.8.1",
    "fflate": "^0.8.2",
    "kokoro-js": "^1.2.1",
    "lucide-preact": "^0.575.0",
    "pdfjs-dist": "^5.4.624",
    "preact": "^10.28.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
